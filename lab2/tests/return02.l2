//test return 4096
int main() {
  bool cond = false;
  int x = 2;
  while (!cond) {
    x <<= 1;
    if (x > 4000)
      //cond = cond || !cond;
      cond = true;
    else
      cond = cond && !cond;
    cond = true;
  }
  return x;
}
